/** @type {import('next').NextConfig} */
const nextConfig = {
  experimental: {
    appDir: true,
  },
  // assetPrefix: 'https://openmesh-expert-landing-page.vercel.app',
}

module.exports = nextConfig
